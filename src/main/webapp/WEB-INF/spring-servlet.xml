<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd">

    <!--
      不能有此项，因为下文有注册过该bean，否则会出现ambiguous报错；
      <context:component-scan base-package="com.tgb.web.controller2"/>
    -->
    <mvc:annotation-driven/>

    <mvc:resources mapping="/resources/**" location="/images/"/>

    <mvc:resources mapping="/resources2/**" location="/css/"/>

    <!--1.获取数据源，配置BasicDataSource-->
    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"></property>
        <property name="url" value = "jdbc:mysql://localhost/db?useUnicode=true&amp;useJDBCCompliantTimezoneShift=true&amp;useLegacyDatetimeCode=false&amp;serverTimezone=UTC&amp;useSSL=true"></property>
        <property name="username" value="root"></property>
        <property name="password" value="199011081108"></property>
    </bean>

    <!--2.给jdbc模板注入数据源，配置dataSource，使SimpleJdbcTemplate正常工作-->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"></property>
        <!--也可以代替以：<constructor-arg ref="dataSource" />  -->
    </bean>

    <!--3.给PersonDaoImpl注入模板，装配PersonDaoImpl的jdbcTemplate属性-->
    <bean id="JdbcPersonDao" class="cn.rayest.phonebook.dao.JdbcPersonDao">
        <property name="jdbcTemplate" ref="jdbcTemplate"></property>
    </bean>

    <!--给控制器注入Dao类-->
    <!--1.1. AddController: -->
    <bean id="AddController" class="cn.rayest.phonebook.controller.AddController">
        <property name="JdbcPersonDao" ref="JdbcPersonDao"></property>
    </bean>

    <!--1.2. 配置映射响应器，映射响应器会根据请求文件来决定进入到哪个控制器，-->
    <bean id="mapping1" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <!-- 这里的key就是你的action请求，后面是要分派的控制器-->
                <prop key="add">AddController</prop>
            </props>
        </property>
    </bean>

    <!--2.1. LoadController: -->
    <bean id="LoadController" class="cn.rayest.phonebook.controller.LoadController">
        <property name="JdbcPersonDao" ref="JdbcPersonDao"></property>
    </bean>

    <!--2.2. 配置映射响应器，映射响应器会根据请求文件来决定进入到哪个控制器，-->
    <bean id="mapping2" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <!-- 这里的key就是你的action请求，后面是要分派的控制器-->
                <prop key="load">LoadController</prop>
            </props>
        </property>
    </bean>

    <!--3.1. UpdateController: -->
    <bean id="UpdateController" class="cn.rayest.phonebook.controller.UpdateController">
        <property name="JdbcPersonDao" ref="JdbcPersonDao"></property>
    </bean>

    <!--3.2. 配置映射响应器，映射响应器会根据请求文件来决定进入到哪个控制器，-->
    <bean id="mapping3" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <!-- 这里的key就是你的action请求，后面是要分派的控制器-->
                <prop key="update">UpdateController</prop>
            </props>
        </property>
    </bean>
    <!--4.1. DeleteController: -->
    <bean id="DeleteController" class="cn.rayest.phonebook.controller.DeleteController">
        <property name="JdbcPersonDao" ref="JdbcPersonDao"></property>
    </bean>

    <!--4.2. 配置映射响应器，映射响应器会根据请求文件来决定进入到哪个控制器，-->
    <bean id="mapping4" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <!-- 这里的key就是你的action请求，后面是要分派的控制器-->
                <prop key="delete">DeleteController</prop>
            </props>
        </property>
    </bean>
    <!--5.1. FindByNameController: -->
    <bean id="FindByNameController" class="cn.rayest.phonebook.controller.FindByNameController">
        <property name="JdbcPersonDao" ref="JdbcPersonDao"></property>
    </bean>

    <!--5.2. 配置映射响应器，映射响应器会根据请求文件来决定进入到哪个控制器，-->
    <bean id="mapping5" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <!-- 这里的key就是你的action请求，后面是要分派的控制器-->
                <prop key="find">FindByNameController</prop>
            </props>
        </property>
    </bean>


    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
</beans>
